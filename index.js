/**
 * @format
 */

import 'react-native-get-random-values';
import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './frontend/react-native.todo.flex/app.json';

AppRegistry.registerComponent(appName, () => App);
